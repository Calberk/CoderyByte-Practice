function longestSubstring(s){
    const sArray = s.split('')
    const longestSubstring = [];
    while(sArray.length){
        const currentStr = [""];
        if(currentStr.indexOf(s[i])===-1){
            currentStr.push(s[i])
            i++
        }else{
            
        }
    }
}